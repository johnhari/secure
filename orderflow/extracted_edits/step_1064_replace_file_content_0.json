{
  "AllowMultiple": "false",
  "Description": "\"Upgrade footprint profile to support Bid/Ask and Total Profile modes with POC highlighting and premium gradient bars.\"",
  "EndLine": "6057",
  "Instruction": "\"Replace _buildFootprintList method to implement POC, dual-gradient stacked bar for total profile view, and transparent-to-solid gradient bars for Bid/Ask view with POC highlight borders.\"",
  "ReplacementContent": "\"  Widget _buildFootprintList(CandleModel c) {\\n    final footprint = _getOrGenerateFootprint(c);\\n    final sortedPrices = footprint.keys.toList()..sort((a, b) => b.compareTo(a));\\n\\n    // Calculate maximum volumes and Point-of-Control (POC)\\n    int maxLevelVol = 1;\\n    int maxTotalVol = 1;\\n    double pocPrice = sortedPrices.isNotEmpty ? sortedPrices.first : -1.0;\\n\\n    for (final p in sortedPrices) {\\n      final data = footprint[p]!;\\n      if (data.buyVolume > maxLevelVol) maxLevelVol = data.buyVolume;\\n      if (data.sellVolume > maxLevelVol) maxLevelVol = data.sellVolume;\\n      \\n      final totalVol = data.buyVolume + data.sellVolume;\\n      if (totalVol > maxTotalVol) {\\n        maxTotalVol = totalVol;\\n        pocPrice = p;\\n      }\\n    }\\n\\n    final priceFormatter = c.symbol.contains('BANKNIFTY') \\n        ? intl.NumberFormat('###,##0') \\n        : intl.NumberFormat('###,##0.00');\\n\\n    return ListView.builder(\\n      itemCount: sortedPrices.length,\\n      padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 4),\\n      itemBuilder: (context, idx) {\\n        final p = sortedPrices[idx];\\n        final data = footprint[p]!;\\n        final totalVolume = data.buyVolume + data.sellVolume;\\n        final isPoc = p == pocPrice;\\n        \\n        final isBuyImbalance = data.buyVolume >= 3 * data.sellVolume && data.buyVolume > 50;\\n        final isSellImbalance = data.sellVolume >= 3 * data.buyVolume && data.sellVolume > 50;\\n\\n        // Custom price column widget\\n        final priceWidget = Container(\\n          width: 76,\\n          alignment: Alignment.center,\\n          margin: const EdgeInsets.symmetric(horizontal: 8),\\n          decoration: BoxDecoration(\\n            color: isPoc \\n                ? AppTheme.goldColor.withValues(alpha: 0.18) \\n                : const Color(0xFF0C1420), // Solid dark background so background bar doesn't bleed through\\n            borderRadius: BorderRadius.circular(4),\\n            border: Border.all(\\n              color: isPoc ? AppTheme.goldColor : \n<truncated 9704 bytes>",
  "StartLine": "5933",
  "TargetContent": "\"  Widget _buildFootprintList(CandleModel c) {\\n    final footprint = _getOrGenerateFootprint(c);\\n    final sortedPrices = footprint.keys.toList()..sort((a, b) => b.compareTo(a));\\n\\n    int maxLevelVol = 1;\\n    for (final p in sortedPrices) {\\n      final data = footprint[p]!;\\n      if (data.buyVolume > maxLevelVol) maxLevelVol = data.buyVolume;\\n      if (data.sellVolume > maxLevelVol) maxLevelVol = data.sellVolume;\\n    }\\n\\n    final priceFormatter = c.symbol.contains('BANKNIFTY') \\n        ? intl.NumberFormat('###,##0') \\n        : intl.NumberFormat('###,##0.00');\\n\\n    return ListView.builder(\\n      itemCount: sortedPrices.length,\\n      padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 4),\\n      itemBuilder: (context, idx) {\\n        final p = sortedPrices[idx];\\n        final data = footprint[p]!;\\n        \\n        final isBuyImbalance = data.buyVolume >= 3 * data.sellVolume && data.buyVolume > 50;\\n        final isSellImbalance = data.sellVolume >= 3 * data.buyVolume && data.sellVolume > 50;\\n\\n        return Padding(\\n          padding: const EdgeInsets.symmetric(vertical: 4),\\n          child: SizedBox(\\n            height: 26,\\n            child: Row(\\n              children: [\\n                Expanded(\\n                  child: Stack(\\n                    alignment: Alignment.centerRight,\\n                    children: [\\n                      Positioned.fill(\\n                        child: Align(\\n                          alignment: Alignment.centerRight,\\n                          child: FractionallySizedBox(\\n                            widthFactor: (data.buyVolume / maxLevelVol).clamp(0.01, 1.0),\\n                            child: Container(\\n                              decoration: BoxDecoration(\\n                                color: AppTheme.bullColor.withValues(alpha: isBuyImbalance ? 0.35 : 0.12),\\n                                borderRadius: const BorderRadius.horizontal(left: Radius.circular(2)),\\n                                border: isBuyImbalance ? Border.all\n<truncated 3506 bytes>",
  "TargetFile": "\"C:\\\\Users\\\\PUTIN\\\\Desktop\\\\ADVANCEORDERFLOW\\\\orderflow\\\\lib\\\\presentation\\\\screens\\\\chart_screen.dart\"",
  "toolAction": "\"Replacing footprint list code\"",
  "toolSummary": "\"Replace footprint list code\""
}