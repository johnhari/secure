{
  "Description": "\"Implement sequential index map and formatted date labels caching in _buildChart to avoid formatting overhead during price-tick smoothing animations.\"",
  "Instruction": "\"Add caching fields to _ChartScreenState and refactor _buildChart to reuse cached data.\"",
  "ReplacementChunks": "[{\"AllowMultiple\":false,\"EndLine\":160,\"ReplacementContent\":\"  int _lastKnownCandleCount = 0;\\n\\n  // Caching for fast chart rendering\\n  List<CandleModel>? _lastProcessedCandles;\\n  final Map<String, int> _cachedCandleIndexMap = {};\\n  final List<String> _cachedAxisLabels = [];\",\"StartLine\":158,\"TargetContent\":\"  bool _newCandleAvailable = false;\\n  int _lastKnownCandleCount = 0;\"},{\"AllowMultiple\":false,\"EndLine\":2252,\"ReplacementContent\":\"  Widget _buildChart(List<CandleModel> candles, double animatedClose, bool isSuperuser) {\\n    if (candles.isNotEmpty) {\\n      debugPrint('[_buildChart] Rendering ${candles.length} candles. Last close: ${candles.last.close}');\\n    }\\n    const bgColor = AppTheme.bgColor;\\n    const gridColor = Color(0xFF2A2E39);\\n    \\n    // Check if we can reuse cached index map and axis labels\\n    final bool canReuseCache = _lastProcessedCandles != null &&\\n        _lastProcessedCandles!.length == candles.length &&\\n        (candles.isEmpty || _lastProcessedCandles!.last.candleKey == candles.last.candleKey);\\n\\n    if (!canReuseCache) {\\n      _cachedCandleIndexMap.clear();\\n      _cachedAxisLabels.clear();\\n      String? prevDayKey;\\n      for (var i = 0; i < candles.length; i++) {\\n        final c = candles[i];\\n        final key = c.candleKey;\\n        _cachedCandleIndexMap[key] = i;\\n        final dayKey = '${c.timeStart.year}-${c.timeStart.month}-${c.timeStart.day}';\\n        if (prevDayKey != null && prevDayKey != dayKey) {\\n          // First candle of a new session \u2014 show date label\\n          _cachedAxisLabels.add(intl.DateFormat('dd MMM').format(c.timeStart));\\n        } else {\\n          _cachedAxisLabels.add(intl.DateFormat('HH:mm').format(c.timeStart));\\n        }\\n        prevDayKey = dayKey;\\n      }\\n      _lastProcessedCandles = List.from(candles);\\n    }\\n\\n    final candleIndexMap = _cachedCandleIndexMap;\\n    final axisLabels = _cachedAxisLabels;\",\"StartLine\":2227,\"TargetContent\":\"  Widget _buildChart(List<CandleModel> candles, double animatedClose, bool isSuperuser\n<truncated 1047 bytes>",
  "TargetFile": "\"c:\\\\Users\\\\PUTIN\\\\Desktop\\\\ADVANCEORDERFLOW\\\\orderflow\\\\lib\\\\presentation\\\\screens\\\\chart_screen.dart\"",
  "toolAction": "\"Caching chart axes details in chart_screen.dart\"",
  "toolSummary": "\"Edit chart_screen.dart\""
}